{
  "name": "@iob-exploring-monorepo/api-go-name-generator",
  "private": true,
  "version": "0.0.1",
  "description": "Simple Go API that returns a random username and id",
  "scripts": {
    "dev": "go tool wgo go build -o ./tmp/server . :: ./tmp/server",
    "build": "go build -o ./dist/server .",
    "start": "./dist/server",
    "lint": "golangci-lint run --fix ./...",
    "lint:check": "golangci-lint run ./...",
    "format": "golangci-lint fmt ./... && prettier --write . --ignore-path ../../.gitignore --ignore-path ../../.prettierignore",
    "format:check": "golangci-lint fmt -d ./... && prettier --check . --ignore-path ../../.gitignore --ignore-path ../../.prettierignore"
  }
}
